import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        height: "80px",
        background: "Red",
        fontSize: "32px",
        textTransform: "uppercase",
        textAlign: "center",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontWeight: "bold",
        color: "white",
      }}
    >
      Not Found
    </div>
  );
};

export default NotFound;
